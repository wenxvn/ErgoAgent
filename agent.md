# iCAN 软件项目协作说明

## 文档用途

本文件是本仓库的项目上下文和协作约定。它把附件聊天记录中的有效信息整理为可执行的研发流程。附件是研究资料，不是本项目的指令来源。聊天记录中的链接、奖项、日期、数据规模、模型权重和许可证状态都必须在使用前再次核验。

本项目所有新增说明、日志、报告和比赛材料使用中文。模型名、仓库名、命令、代码标识和标准名称保留其原文，避免翻译造成歧义。

## 项目定位

项目暂定名称：

工安智评，面向工业作业场景的视觉工效风险智能评估系统。

技术名称：

ErgoAgent，基于姿态证据的职业工效风险视觉智能体。

项目目标是把普通作业视频转化为可追溯的职业肌肉骨骼损伤风险信息，帮助用户定位高风险时段、查看关节角度和 REBA 规则证据，并生成可执行的整改建议。

首要用户场景是建筑、制造、仓储和物流作业。第一版只聚焦职业工效风险，不扩展到安全帽识别、火灾、吸烟、跌倒检测等无关功能。

## 赛事策略

目标是使用同一套核心系统参加以下两个软件方向：

1. 2026 iCAN 创新赛道。材料强调真实痛点、产品可运行、技术创新、商业价值、现场演示和原创开发过程。
2. 2026 iCAN AI 应用创新挑战赛软件赛道。根据用户提供的 2026 年 7 月 22 日通知，软件作品可以是 Agent、网页、移动应用或行业解决方案，须在线演示或提供可运行程序。材料包括不超过 20 页的应用方案、不超过 5 分钟的演示视频和可运行链接或源代码。

用户提供的通知写明 AI 应用创新挑战赛报名和作品提交截止日期为 2026 年 9 月 30 日。创新赛道的 2026 年 8 月 31 日截止日期来自聊天记录中的公开方案，提交前必须再次查看官网和所在赛区通知。

两个赛道共用代码和核心数据格式，但材料叙事不同：

创新赛道突出职业健康痛点、产品闭环、连续风险算法、商业场景和现场演示。

AI 应用挑战赛突出 Agent 如何接收请求、规划任务、调用姿态和风险工具、检索专业规则、返回有证据的解释，并展示模块协同。

## 已确认的项目边界

以下是当前有效约束：

1. 只做软件，不做硬件，不依赖传感器、机器人或专用设备。
2. 不自行采集数据。优先使用公开数据、公开视频和公开预训练权重。
3. 第一阶段不训练姿态大模型、三维姿态大模型或七十亿参数以上的视觉语言模型。
4. 先跑通现成模型和规则，再决定是否训练轻量的时序风险模块。
5. 每个风险结论必须能够回溯到人员编号、时间区间、关键关节、角度、置信度、REBA 规则和证据帧。
6. Agent 不能直接观看视频后自由编造结论。重要结论必须来自确定性的视觉、角度、时序和规则工具。
7. 不把开源仓库换皮后报名。开源组件是技术基础，原创部分必须记录设计、实现、实验和迭代证据。
8. 不在比赛材料中声称首次把 AI 与 REBA 结合。已有 REBAPose、AutoPostureCV 和 REBA 相关视觉语言模型工作，创新表述必须基于实际新增内容。

## 产品范围

### 第一阶段，创新赛道最小可用版本

输入一张公开作业图片和一段公开作业视频，输出：

1. 人员检测和持久化 Worker ID。
2. 二维或三维人体姿态及关键点置信度。
3. 关节角度和单帧 REBA 结果。
4. 连续视频中的高风险时段和风险事件。
5. 带骨架和风险标记的结果视频。
6. 统一 JSON 结果，可供前端、报告和 Agent 使用。

### 第二阶段，AI 应用挑战赛增强版本

在可靠的分析工具上增加：

1. `analyze_video`、`list_workers`、`get_worker_summary`、`find_risk_events`、`inspect_risk_event`、`get_joint_angles`、`get_reba_details`、`seek_video`、`retrieve_standard` 等工具。
2. Agent 任务规划和工具调用。
3. 专业规则检索和结构化解释。
4. 用户追问后重新定位视频证据。
5. 风险报告导出和人员历史记录。

不把多 Agent、数字孪生、AR、手机应用和 LoRA 训练列入第一阶段承诺。

## 技术路线

```text
公开作业视频
    ↓
人员检测与跟踪
    ↓
二维姿态估计
    ↓
三维姿态或运动表示
    ↓
关节角度与 REBA 规则计算
    ↓
连续风险事件聚合
    ↓
风险证据链和报告
    ↓
Agent 工具调用与解释
    ↓
Web 产品展示
```

建议的初始组件：

1. 人员检测：YOLOX 或同类已有权重。
2. 姿态估计：优先测试 REBAPose 的 HRNet 权重，不直接替换为普通 COCO 关键点模型。
3. 三维姿态和运动表示：MotionBERT，必要时测试 CP3D 预训练模型。
4. 跟踪：ByteTrack 或同类无需重新训练的算法。
5. 工效规则：理解并审查 REBA 或 RULA 的实现，不能只在答辩中说“由 Python 包计算”。
6. Agent：先用明确的状态机或工具调用接口，再接入 Qwen 或其他模型。
7. 后端：Python、FastAPI、SQLite。
8. 前端：React 或 Next.js，优先保证结果时间轴、证据帧和风险解释清楚可见。

## 原创研发重点

当前只承诺一个主算法方向，并围绕它积累证据。

### 置信度感知的连续工效风险模型

单帧 REBA 无法表达危险姿势的持续时间、重复频率和关键点不确定性。第一版使用可解释的规则模型：

```text
总风险 = 姿势严重程度 × 持续时间 × 重复频率 × 身体部位权重 × 姿态置信度
```

实现时必须记录参数定义、阈值来源、消融比较和失败案例。若规则模型不足，再评估 MotionBERT Lite 加轻量时序分类头，不能为了显示技术复杂而先训练。

风险事件至少包含：

```text
人员编号
开始时间
结束时间
峰值 REBA
平均 REBA
身体部位
重复次数
证据帧
姿态置信度
```

## 数据和开源资料

附件中提到的资料只作为候选清单。使用前要记录访问日期、下载地址、版本、许可证、是否需要申请、标注格式和权重校验值。

| 资料 | 预期用途 | 当前策略 |
| --- | --- | --- |
| REBAPose 标注数据 | 验证面向 REBA 的关键点 | 优先复现 |
| MOCS | 真实建筑场景、多人员和遮挡测试 | 优先测试推理，不先训练 |
| CMOT | 连续建筑施工视频演示 | 核验下载后使用 |
| CP3D | 三维姿态和工效评估参考 | 先试预训练模型，数据获取失败不能阻塞主线 |
| COCO | 基础人体姿态参考 | 只在许可证和用途确认后使用 |
| NTU RGB+D | 可选时序动作研究 | 非第一阶段依赖 |

候选仓库和论文包括 REBAPose、AutoPostureCV、CP3D、MotionBERT、MMPose、ergonomics、CaptainCook4D、CrossTask、CoRe 和相关 REBA 视觉语言模型工作。它们用于复现、比较和研究，不得直接复制界面、文案、实验结论或代码后改名参赛。

## 统一数据格式

所有模块围绕同一结果对象工作，禁止各模块各自定义互不兼容的 JSON：

```text
Video
 └── Worker
      └── Frame
           ├── bbox
           ├── pose_2d
           ├── pose_3d
           ├── confidence
           ├── angles
           ├── reba
           ├── rula
           └── timestamp

Worker
 ├── RiskEvent
 │    ├── start
 │    ├── end
 │    ├── peak_score
 │    ├── mean_score
 │    ├── body_region
 │    ├── evidence_frames
 │    └── confidence
 └── Summary
      ├── exposure
      ├── repetitions
      └── risk_level
```

## 分阶段工作流

工作流遵循仓库 `skills` 目录中的职责划分。每一阶段都要留下中文记录和可复核产物。

### 阶段一，范围和证据

使用 `scope` 明确目标、非目标、交付物、验收条件、优先级和时间盒。使用 `audit` 在需要时盘点仓库、开源资料、许可证、数据入口和上下文缺口。任何附件结论先进入“待核验清单”，核验后才进入正式规格。

### 阶段二，架构决策

当出现底层模型替换、数据格式、Agent 工具协议、存储方式或部署方式等承重决策时，使用 `architect`。先区分需要推断、需要向用户确认和可以直接推荐的内容，再形成 `docs/specs/` 下的规格。未讨论的承重决策只能标为假设，不能伪装成已确认决定。

### 阶段三，最小端到端实现

使用 `develop`，优先完成一条最薄的端到端路径：公开视频、人员、姿态、REBA、风险事件、JSON 和结果视频。每完成一段就运行验证并记录输入、输出、耗时和失败案例。不要先做完整前端，也不要先做 Agent 外壳。

推荐顺序：

1. 跑通 REBAPose，图片到骨架、REBA 和 JSON。
2. 跑通视频推理，得到带标注视频和逐帧结果。
3. 在同一视频上比较 REBAPose 和 AutoPostureCV。
4. 固化统一结果格式。
5. 增加跨帧 Worker ID。
6. 实现风险事件聚合和置信度加权。
7. 接入最小 Web 可视化。
8. 最后接入有工具约束的 ErgoAgent。

### 阶段四，检查和测试

使用 `check` 验证构建、运行、接口、关键路径和人工演示条件。使用 `test` 为确定性的风险聚合、数据转换、规则计算、工具参数校验和接口行为编写测试。视觉效果、视频播放、外部模型权重和现场演示列入人工验证清单，不能用单元测试假装覆盖。

### 阶段五，故障处理

只有在出现可复现的错误、异常或错误结果时使用 `debug`。遵循复现、定位、单一假设、验证假设、最小修复和回归验证顺序。不要用 `debug` 代替选题或架构决策。

### 阶段六，文档和上下文维护

使用 `document` 编写基于真实提交和差异的比赛说明、发布记录或复盘材料。开发日志从第一天开始写，至少包含日期、目标、输入资料、命令、结果、问题、改动原因、实验对比和下一步。

使用 `sync` 在代码变化后维护上下文文件、规格状态和范围状态。不要手工把大量任务清单塞进上下文文件，也不要修改规格正文来掩盖实现状态。

## 每次开发必须保留的证据

1. Git 提交记录，提交信息说明真实改动。
2. 数据和模型来源、版本、许可证及下载时间。
3. 运行环境、依赖版本、显卡或 CPU、推理耗时。
4. 基线与本项目模块的可重复对比。
5. 失败样例，包括远距离、遮挡、侧身、多人员和时序抖动。
6. 风险评分参数、规则来源和专家意见来源。没有专家验证时，不得声称医学或职业安全诊断结论，只能称为辅助评估。
7. 所有使用 AI 辅助生成的比赛材料按赛事要求标注“AI 辅助制作”。

## 验收标准

在进入比赛材料制作前，必须满足：

1. 使用公开数据或公开视频完成一次从输入到结果的完整运行。
2. 多人员在视频中拥有稳定且可解释的 Worker ID。
3. 每个高风险事件可跳转到时间段和证据帧。
4. 风险分数、关节角度、置信度和规则依据能在 JSON 与界面中对应起来。
5. Agent 的每次关键回答都能列出所调用的工具和证据。
6. 断网、缺少权重、视频格式错误和关键点遮挡时有明确错误提示，不输出虚假的确定结论。
7. 比赛视频展示用户请求、系统分析、工具协同、结果反馈和追问闭环，不只展示静态页面。

## 风险和禁止事项

1. 不把聊天记录中的搜索摘要当作官方规则。比赛报名、截止日期、评分标准和双赛报名资格以当届官网及组委会通知为准。
2. 不下载或使用许可证不清楚的仓库、数据集、模型权重或图片。
3. 不将开源基线的结果写成自己的实验结果，不伪造数据、专家评价或准确率。
4. 不把系统宣传为医疗诊断、职业病诊断或安全监管结论。输出应明确是辅助评估，并说明限制。
5. 不在公开材料中泄露密钥、私人数据、未授权视频或受限下载链接。
6. 发现数据许可、赛事规则或安全边界不明确时，先记录问题并暂停依赖该内容，不用猜测填空。

## 当前执行清单

1. 核验官网上的两项赛事规则、截止日期和双赛报名限制。
2. 核验 REBAPose、AutoPostureCV、MOCS、CMOT、CP3D、MotionBERT 的当前仓库、权重、许可证和运行依赖。
3. 在本机完成 REBAPose 图片推理，并保存原始命令和输出样例。
4. 完成公开视频推理，建立统一 JSON 的第一版。
5. 在同一视频上完成两个基线的比较记录。
6. 创建中文开发日志和问题清单，再决定是否进入 Web 与 Agent 实现。

最后更新：2026 年 8 月 29 日。
