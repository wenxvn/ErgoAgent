# 决策记录

## D 001，确定主项目为 ErgoAgent

日期：2026 年 8 月 29 日

背景：需要一个纯软件项目同时适配 2026 iCAN 创新赛道和 AI 应用创新挑战赛软件赛道，并利用计算机视觉和姿态估计基础。

决定：以“工安智评，面向工业作业场景的视觉工效风险智能评估系统”为主项目，技术名称为 ErgoAgent。

理由：职业工效风险有明确行业痛点，姿态估计与 REBA 规则结合自然，公开视频和公开权重可以支持最小版本，Agent 可以围绕结构化证据调用工具。

代价：需要理解人体工效规则、跨帧跟踪、三维姿态和许可证边界，不能只做普通姿态展示。

复核条件：如果在公开施工视频上的姿态和跟踪无法达到可演示稳定性，先缩小到单人短视频和离线分析，不改变项目主题。

## D 002，第一阶段不训练大模型

日期：2026 年 8 月 29 日

决定：第一阶段使用公开预训练权重和确定性规则，暂不训练姿态大模型、三维姿态大模型或大型视觉语言模型。

理由：先验证端到端路径和产品价值，把时间用于证据链、连续风险模型和可演示产品。

复核条件：只有在基线对比证明现成模型无法支持核心指标时，才评估轻量时序模型或有针对性的微调。

## D 003，数据不自采

日期：2026 年 8 月 29 日

决定：不自行采集数据，优先使用公开数据、公开测试视频和公开权重。

理由：降低时间、隐私和授权风险，符合当前比赛准备约束。

复核条件：任何需要申请或许可证不清的资料都不能成为主线唯一依赖。

## D 004，采用 Tracer Bullet 和 Full 工作流

日期：2026 年 8 月 29 日

决定：以 Tracer Bullet 作为项目构建方式，以 Full 作为默认工作流深度。

理由：项目需要尽早证明视频、姿态、规则、结果、界面和 Agent 可以连通，同时长期维护、外部资料许可和比赛交付要求独立验证与记录。

执行影响：先完成一条真实的最小端到端路径，再逐段增加功能；每个阶段完成后执行验证、测试、独立复核和文档同步。

复核条件：如果比赛时间或资源明显不足，可以对低风险文档和实验功能临时降为 Lean，但数据、规则、Agent 和比赛交付仍保持 Full 要求。

## D 005，采用模块化单体与本地优先架构

日期：2026 年 8 月 29 日

决定：使用单仓库模块化单体，Python 负责后端和视觉分析，React 与 Vite 负责前端，FastAPI 提供 REST JSON 接口，SQLite 保存任务和元数据，本地文件保存视频与证据，独立 Worker 处理长时间分析，Docker Compose 用于本地复现。

理由：当前是绿色项目，目标是单机单用户比赛演示，没有测量到的并发瓶颈或多团队 ownership 边界。该方案能共享数据契约，隔离 GPU 长任务，并保持安装、调试和现场复现简单。

放弃：暂不使用微服务、云对象存储、专用消息队列、WebSocket、账号系统和公网部署。这些能力只有在实际吞吐、多人协作或外部访问需求出现后才单独设计。

复核条件：工程骨架完成后，如果固定样例证明 SQLite 任务调度、单机磁盘或 Worker 性能无法满足演示，再基于数据提出替换规格，不凭空提前扩容。

## D 006，采用关系型核心加受控 JSON 的数据契约

日期：2026 年 8 月 29 日

决定：使用 SQLite 和 SQLAlchemy 保存视频、任务、分析运行、人员、逐帧观察、风险事件、证据帧、结果文件和组件来源。姿态、角度和 REBA 细节使用 Pydantic 校验的 JSON。视频和证据文件只保存在数据根目录，数据库保存相对路径、大小和 SHA256。

理由：项目需要按人员、时间和风险事件查询，并且必须从结论追溯到具体帧和规则。关系表提供外键、唯一约束和分页索引，JSON 保留模型输出变化空间，适合当前单机规模和后续数据库迁移。

放弃：暂不采用全 JSON 文件或专用时序数据库。前者难以分页和关联，后者在没有测量瓶颈前会增加部署和备份成本。

复核条件：当逐帧写入量或查询延迟达到记录的阈值时，再依据实验数据增加摘要表、物化视图或专用存储；不得在没有数据时改变主存储。
